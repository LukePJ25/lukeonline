---
import IconButton from "./IconButton.astro";

const { ...props } = Astro.props;

const entries = props.entries;
---
<div class="flex gap-2 flex-row flex-wrap">
  {entries.map(entry => 
      <IconButton image={entry.image}
                  link={entry.link}
        />
  )}
</div>

