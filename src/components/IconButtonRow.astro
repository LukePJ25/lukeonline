---
import IconButton from './IconButton.astro'

const { ...props } = Astro.props

const entries = props.entries
---

<div class="flex flex-row flex-wrap gap-2">
  {entries.map((entry) => <IconButton image={entry.image} link={entry.link} />)}
</div>
