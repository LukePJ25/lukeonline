---
import LargeButton from './LargeButton.astro'

const { ...props } = Astro.props

const entries = props.entries
---

<div class="grid grid-cols-1 gap-2">
  {
    entries.map((entry) => (
      <LargeButton title={entry.title} desc={entry.desc} link={entry.link} />
    ))
  }
</div>
